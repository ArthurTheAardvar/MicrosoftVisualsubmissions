#include <iostream>
using namespace std;

int main() {

	int myNum[3] = { 10, 20, 30 };

	cout << "The numbers are: ";

	for (const int &n : myNum) {
		cout << n << "  ";
	}

	cout << "\nThe numbers are: ";

	for (int i = 0; i < 5; ++i) {
		cout << myNum[i] << "  ";
	}

	return 0;
}
